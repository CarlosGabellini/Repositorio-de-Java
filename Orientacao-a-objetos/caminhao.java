public class caminhao extends heranca_veiculo {
    
    //Aqui vamos dar uma olhada como funciona a herança;
    //O caminhao recebe todas as funcoes de veiculo, uma vez que usamos extends para fazer isso;

    //La na Main agora acessamos os dados do caminhao pela herança;

    //Tambem podemos usar o super caso queira usar os contrutores ao inves de get e setler;

    /*public class caminhao (String cor, int ano, double velocidade) {
        super(cor, ano, velocidade);                                    //Fica mais ou menos assim;
    } */
}